import { ColorizeUserDirective } from './colorize-user.directive';

describe('ColorizeUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizeUserDirective();
    expect(directive).toBeTruthy();
  });
});
